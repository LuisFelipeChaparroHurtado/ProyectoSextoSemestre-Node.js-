export const SQL_LIST_CUSTOMER = {
    LIST: 'SELECT c.id_customer, c.customer_identification, c.name_customer, c.surname_customer, c.customer_address, c.number_phone_customer \
    FROM customer c'
}